---
- name: install docker, docker-compose and colima
  become: true
  become_user: "{{ docker_host_user }}"
  homebrew:
    name:
      - docker
      - colima
      - docker-compose
    state: present
