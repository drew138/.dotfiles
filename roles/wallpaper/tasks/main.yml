---
- name: force desktop wallpaper via defaults
  ansible.builtin.command: >
    osascript -e 'tell application "System Events" to set picture of every desktop to "{{ macos_wallpaper_path }}"'
  changed_when: false
