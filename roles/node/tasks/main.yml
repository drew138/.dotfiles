---
- name: install node
  become: true
  become_user: "{{ ansible_facts.env.USER }}"
  shell: "source {{ ansible_facts.env.HOME }}/.nvm/nvm.sh && nvm install --lts"
  changed_when: false
  args:
    executable: /bin/bash
