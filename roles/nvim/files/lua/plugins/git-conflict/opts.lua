local Opts = {
	default_mappings = false,
	default_commands = false,
	disable_diagnostics = true,
	list_opener = "copen",
	highlights = {
		incoming = "DiffAdd",
		current = "DiffText",
	},
}

return Opts
