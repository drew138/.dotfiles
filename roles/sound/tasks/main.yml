---
- name: disable system error beep
  ansible.builtin.command: >
    defaults write NSGlobalDomain com.apple.sound.beep.feedback -bool false
  changed_when: false

- name: mute startup chime
  ansible.builtin.command: >
    nvram StartupMute=%01
  become: true
  changed_when: false
